<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.siddharth.tarun_teacher">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
    <activity android:name=".MainActivity"/>
    <activity android:name=".promote" android:parentActivityName=".MainActivity"/>
    <activity android:name=".contactus" />
    <activity android:name=".feedback" />
    <activity android:name=".rateus" android:theme="@style/Theme.AppCompat.Translucent"/>
    <activity android:name=".about" />
    <activity android:name=".signup"/>


    <activity android:name=".intro" >

    </activity>
    <activity android:name=".swipe" >


    </activity>
    <activity android:name=".test" />
    <activity android:name=".MakeStructure" />
       <activity android:name=".singnin">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        </application>
</manifest>
